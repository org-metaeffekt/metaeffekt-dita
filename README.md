# metaeffekt-dita
Provides DITA support integrated in Maven.

## Building this Project

To successfully build this project via maven, additional dependencies are required which are not listed in any
public remote maven repository. To acquire these local dependencies ask KKL.